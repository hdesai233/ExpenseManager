# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# electron-builder output (installer .exe, unpacked app copy — regenerate with `npm run electron:build`)
release/

# Local app data — the real database/secrets live outside this repo entirely (in the OS
# per-user app-data folder, e.g. %APPDATA%\expense-manager on Windows — see Settings screen
# "Storage location"), but these patterns guard against any local/dev copy ever landing here.
*.sqlite
*.sqlite-wal
*.sqlite-shm
ledger.security.json
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
